{% extends "::layout.html.twig" %}
{% block body %}
    <div class="full_w">
        <div class="h_title"></div>
        <div class="entry">
            <div class="sep"></div>
        </div>
        <table>
            <thead>
                <tr>
                    <th scope="col"></th>
                    <th scope="col"></th>
                    <th scope="col"></th>
                    <th scope="col"></th>
                    <th scope="col" style="width: 65px;">تحرير</th>
                </tr>
            </thead>
            <tbody>
                {% for  in  %}
                    <tr {% if loop.index is odd %}class="color"{% endif %}>
                        <td>{{  }}</td>
                        <td>{{  }}</td>
                        <td>{{  }}</td>
                        <td>{{  }}</td>

                        <td>
                            <a href="{{ path('',{ }) }}" class="table-icon edit" title="تحرير"></a>
                            <a href="{{ path('',{ }) }}" class="table-icon delete" title="حذف"></a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        <div class="entry">
            <div class="navigation">
                {{ knp_pagination_render(listeVisiteurs) }}
            </div>
            <div class="sep"></div>		
        </div>
    </div>
{% endblock %}